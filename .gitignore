# Data products (not tracked — rebuild with process_wirewalker_rbr.py)
L1/
L2/
L3/
*.nc
*.rsk

# Raw ADCP files + dolfyn index cache (huge; never commit)
*.ad2cp
*.ad2cp.index
ww_sig1000/test_data/

# Vendored MATLAB reference toolbox (kept local, not distributed)
WW_Velocity_Processing_SWOT/

# Figures (regenerated by the notebook)
figs/
*.png
*.pdf

# Python / Jupyter
__pycache__/
*.pyc
.ipynb_checkpoints/

# Notebooks — analysis / deployment-specific, kept local, not distributed
*.ipynb
notebooks/

# Formal writeup — kept local; the README is the distributed doc
docs/

# Deployment config files — kept local; copy from the tracked config_*.example.json
config_*.json
!config_*.example.json

# OS / editor
.DS_Store
